#라운드로빈 모델 (Round Robin Scheduling)

	CPU 스케줄링 알고리즘 중 하나.
	모든 프로세스에게 동등한 시간 할당(Time Quantum) 을 주고 순환적으로 실행.

# 특징:

	선점형 스케줄링 (시간 다 되면 강제로 다음 프로세스로 교체)

	응답 시간은 빠르지만, 문맥 교환(Context Switching)이 잦으면 비효율적

# 예시:

프로세스: P1, P2, P3
타임 퀀텀: 2초
실행 순서: P1 → P2 → P3 → P1 → P2 → P3 ...