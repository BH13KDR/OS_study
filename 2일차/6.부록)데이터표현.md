#데이터 표현 (Data Representation)

	컴퓨터는 모든 데이터를 이진수(0,1) 로 표현합니다.

# 기본 단위:

	비트(bit) : 0 또는 1

	바이트(byte) : 8비트

	워드(word) : CPU가 한 번에 처리하는 단위 (예: 32bit, 64bit)

# 수 표현:

	정수(Int): 2의 보수(Two’s Complement)

	실수(Float): 부동소수점(Floating Point)

	문자(Str): ASCII, Unicode(UTF-8)